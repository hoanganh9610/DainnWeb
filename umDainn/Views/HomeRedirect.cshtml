@inherits UmbracoViewPage
@{
    IEnumerable<IPublishedContent> rootNode = Umbraco.ContentAtRoot();
    var home = rootNode.FirstOrDefault();

    if (home != null)
    {
        Context.Response.Redirect(home.Url());
    }
    else
    {
        <h2>Website Not Published</h2>
    }
}
